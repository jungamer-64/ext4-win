//! Production-root reachability gate for the release driver LLVM IR.

use std::process::ExitCode;

fn main() -> ExitCode {
    eprintln!("production reachability analysis is not implemented yet");
    ExitCode::FAILURE
}
